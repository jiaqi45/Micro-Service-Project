server:
  port: 8101

spring:

  sleuth:
    sampler:
      probability: 1.0

  zipkin:
    baseUrl: http://localhost:8084


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'

